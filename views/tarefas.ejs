<!DOCTYPE html>
<html>

<head>
    <title>Lista de Tarefas</title>
</head>

<body>
    <h1>Lista de Tarefas</h1>
    <ul>
        <% tarefas.forEach(tarefa=> { %> <li>
                <%= tarefa.title %>
                <%= tarefa.description%>
            </li>
            <% }); %>
    </ul>
    <form action="/tarefas" method="POST">
        <input type="text" name="title" placeholder="Nova Tarefa">
        <input type="text" name="description" placeholder="Descrição da Tarefa">
        <button type="submit">Adicionar uma tarefa</button>
    </form>
</body>

</html>